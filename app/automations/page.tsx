import WorkflowsPage from '../workflows/page';

// Wrapper page: /automations
// Reuses the existing workflows page as Automations.
export default async function AutomationsPage() {
  return <WorkflowsPage />;
}
